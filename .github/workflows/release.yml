# DEPRECATED: This workflow has been replaced by the new CI/CD architecture:
# - build.yml (unsigned builds for all environments)
# - sign-release.yml (signs production builds with release key)
#
# This workflow is kept temporarily for backward compatibility.
# See docs/VERSIONING.md for the new signing architecture documentation.

name: Release - Sign and Publish [DEPRECATED]

on:
  # Disabled - replaced by build.yml + sign-release.yml
  workflow_dispatch: {}

permissions:
  contents: read

jobs:
  deprecated:
    name: Deprecated Workflow
    runs-on: ubuntu-latest
    steps:
      - name: Notice
        run: |
          echo "This workflow is deprecated and has been replaced by:"
          echo "  - build.yml (unsigned builds)"
          echo "  - sign-release.yml (production signing)"
          echo ""
          echo "For new releases, push a git tag matching v*.*.* pattern."
          echo "The build.yml and sign-release.yml workflows will handle signing and publishing."
          exit 1
